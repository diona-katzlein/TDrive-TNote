{
  "name": "tdrive",
  "version": "0.1.0",
  "description": "TDrive — jadikan akun Telegram sebagai cloud drive (Node.js + Express + EJS + SQLite)",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "test": "node --test test/*.test.js",
    "test:security": "node --test test/security.test.js",
    "check:syntax": "node --check src/app.js && node --check src/config/security.js && node --check src/services/sessionService.js && node --check src/services/backupService.js && node --check src/services/reconciliationService.js && node --check src/services/webdavSecurity.js && node --check src/routes/webdav.js && node --check src/routes/backup.js && node --check src/routes/reconciliation.js",
    "migrate": "node src/db/migrate.js",
    "migrate:check": "node src/db/migrate.js --dry-run",
    "genkey": "node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "base32": "^0.0.7",
    "better-sqlite3": "^11.3.0",
    "big-integer": "^1.6.52",
    "dotenv": "^16.4.5",
    "ejs": "^3.1.10",
    "express": "^4.19.2",
    "express-mysql-session": "^3.0.3",
    "express-session": "^1.18.0",
    "mammoth": "^1.12.0",
    "multer": "^2.2.0",
    "mysql2": "^3.22.5",
    "telegram": "^2.22.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
